def main(private field[70] vector, private field position, field number_elements) -> (field): 
    field result = 0
    field min = vector[position]
    
    for field i in 0..70 do
        result = if vector[i] >= min then result else result + 1 fi
    endfor
    
    result == 70 - number_elements
    return 11