import "../modules/512bitPacked.code" as sha256packed

def main(private field[4] x, private field[4] y, private field[4] z, field h0, field h1) -> (field):
    h = sha256packed(x, y, z)
    h[0] == h0
    h[1] == h1
    
    return 1